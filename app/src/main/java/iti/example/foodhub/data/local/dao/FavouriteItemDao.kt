package iti.example.foodhub.data.local.dao

import androidx.room.Delete
import androidx.room.Insert
import androidx.room.Query

interface FavouriteItemDao /*{
   @Query("SELECT *FROM favorite_items")
    suspend fun getAllFavouriteItems():List<FavouriteItems>
    @Insert
    suspend fun  insertFavouriteItems(item:FavouriteItems)
    @Delete
    suspend fun deleteFavouriteItem(item:FavouriteItems)
}*/